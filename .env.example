# Gym Tracker - Environment Variables
# Copy this file to .env and fill in your values

# =============================================================================
# GYM PORTAL CONFIGURATION
# =============================================================================

# Gym Portal URL (eFitness CMS portal for your gym)
# Find your gym's URL by logging in via web browser and copying the base URL
GYM_URL=https://your-gym.cms.efitness.com.pl

# Gym Login Credentials
GYM_EMAIL=your_email@example.com
GYM_PASSWORD=your_password_here

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================

# Admin API Secret (generate with: openssl rand -hex 32)
# Required for admin endpoints and data export
ADMIN_SECRET=generate_random_secret_here

# Admin User Password (optional)
# If not set, a secure random password will be generated on first startup
# ADMIN_PASSWORD=your_secure_admin_password

# CORS Allowed Origins (comma-separated, default: * allows all)
# Example: https://your-domain.com,http://localhost:5000
# ALLOWED_ORIGINS=*

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Server Port (default: 5000)
# PORT=5000
